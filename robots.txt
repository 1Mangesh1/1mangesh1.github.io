# Robots.txt for mangeshbide.tech
# Allow all crawlers to access all content

User-agent: *
Allow: /

# Block access to admin/sensitive areas and maintenance
Disallow: /.well-known/
Disallow: /api/
Disallow: /maintenance

# Dev artifacts (/benchmark-reflow.html, /skeleton-demo) are deliberately NOT
# disallowed here: they carry a noindex meta tag, and a blocked crawl would stop
# that tag from ever being read.

# Sitemap location
Sitemap: https://mangeshbide.tech/sitemap-index.xml

# No global Crawl-delay: the site is static behind Cloudflare, so there is no
# origin to protect. Google ignores the directive anyway, while Bing and the AI
# crawlers honour it — it only ever slowed down the crawlers worth having.

# Specific rules for different bots
User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

User-agent: Slurp
Allow: /

# Block specific bad bots
User-agent: AhrefsBot
Disallow: /

User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: / 